function getActivePaylines() {
    // determine which paylines the user activated before the spin
    // use this as the length of the payline array
    let activePaylines = 30;
    return activePaylines;
}